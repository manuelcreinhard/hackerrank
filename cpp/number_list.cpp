#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
	int cases; cin >> cases;
	int n, k, val;
	while(cases--){
		cin >> n >> k;
		vector<int> list;
		for(int i = 0; i < n; i++){
			cin >> val;
			list.push_back(val);
		}
	}
    return 0;
}
