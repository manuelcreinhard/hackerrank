#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
	int search;
	scanf("%d",&search);
	int size;
	scanf("%d",&size);
	char *arr = malloc(sizeof(char)*100000);
	scanf("%[^\n]s",arr);
    return 0;
}

